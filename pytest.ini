[pytest]
pythonpath = src
testpaths = tests
filterwarnings =
    error
markers =
    postgis: requires DATABASE_URL and a running PostGIS instance
# FLOODLENS_DEMO_FIXTURES=0 is applied in tests/conftest.py (pytest has no native env=).
